# Example .env file for FitFlex main_frontend (React)

# URL for the backend API (must include API version path, e.g. /api/v1)
REACT_APP_API_BASE_URL=http://localhost:5000/api/v1

# (No secrets should be committed; provide example values only)
