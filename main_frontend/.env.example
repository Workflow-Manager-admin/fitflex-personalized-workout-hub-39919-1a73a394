# FitFlex Frontend Environment Variables
# Copy this file to `.env` and set the correct URL before running.

# Base URL for backend API endpoints (should match Express backend)
REACT_APP_API_BASE_URL=http://localhost:5000/api/v1
